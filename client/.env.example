# Local dev: leave unset — requests go to /api and Vite proxies to localhost:5000
# Production (Vercel): set this to your deployed Render backend, including /api
VITE_API_URL=https://your-backend.onrender.com/api
